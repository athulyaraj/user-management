package com.user.management.app.enums;

/**
 * Access type of resources. If access type is ANY, the resource can be accessed by anyone. if it is ACCESS_ONLY, users having the access can only access it
 **/
public enum AccessType {
    ANY,ACCESS_ONLY
}
